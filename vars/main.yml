---
# graalvm github url
graalvm_github_url: "https://github.com/graalvm/graalvm-ce-builds/releases/download/jdk-{{ graalvm_java_version }}"

# The root folder of this GraalVM installation
graalvm_home: "{{ graalvm_install_dir }}/jdk-{{ graalvm_java_version }}"

graalvm_redis_sha256sum_url: ''

redis_sha256sum: ''